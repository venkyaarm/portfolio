import React from "react";
import styled, { keyframes } from "styled-components";
import { FaGithub, FaLinkedin, FaInstagram, FaEnvelope, FaPhone } from "react-icons/fa";

const Section = styled.section`
  padding: 6rem 10%;
  background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
  color: #fff;
  text-align: center;
  border-radius: 20px;
  box-shadow: 0 0 30px rgba(0, 255, 255, 0.1);
  margin: 3rem auto;

  h2 {
    font-size: 3rem;
    color: #00ffff;
    margin-bottom: 2rem;
    position: relative;
    display: inline-block;

    &::after {
      content: "";
      position: absolute;
      bottom: -10px;
      left: 50%;
      transform: translateX(-50%);
      width: 80px;
      height: 4px;
      background: #ffcc00;
      border-radius: 2px;
    }
  }
`;

const ContactInfo = styled.div`
  margin-bottom: 2rem;
  font-size: 1.2rem;
  a {
    color: #00ffff;
    text-decoration: none;
    transition: color 0.3s;
    &:hover {
      color: #ffcc00;
    }
  }
`;

const IconGrid = styled.div`
  display: flex;
  justify-content: center;
  gap: 2rem;
  flex-wrap: wrap;
`;

const IconWrapper = styled.a`
  background: rgba(255, 255, 255, 0.05);
  padding: 1rem;
  border-radius: 50%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 2rem;
  color: #00ffff;
  transition: all 0.3s ease;
  box-shadow: 0 0 15px rgba(0, 255, 255, 0.2);

  &:hover {
    transform: scale(1.2) rotate(10deg);
    color: #ffcc00;
    box-shadow: 0 0 25px rgba(255, 204, 0, 0.4);
    background: rgba(255,255,255,0.1);
  }

  @media (max-width: 500px) {
    font-size: 1.8rem;
    padding: 0.8rem;
  }
`;

export default function Contact() {
  return (
    <Section id="contact">
      <h2>Contact Me</h2>
      <ContactInfo>
        <p><FaEnvelope /> <a href="mailto:venkyaam@mail.com">venkyaam@mail.com</a></p>
        <p><FaPhone /> +91 81255 22139</p>
      </ContactInfo>
      <IconGrid>
        <IconWrapper href="https://github.com/venkyaarm" target="_blank">
          <FaGithub />
        </IconWrapper>
        <IconWrapper href="https://linkedin.com/in/venkatesh-kuncham-120531307" target="_blank">
          <FaLinkedin />
        </IconWrapper>
        <IconWrapper href="https://instagram.com/yourinstagram" target="_blank">
          <FaInstagram />
        </IconWrapper>
      </IconGrid>
    </Section>
  );
}
